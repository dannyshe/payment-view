spring:
  application:
    name: paymentview
  main:
    allow-bean-definition-overriding: true

management:
  endpoints:
    web:
      exposure:
        include:
          - "info"
          - "health"

